@page
@model LMS.Pages.Submission.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Submission</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Submission.ID" />
            <div class="form-group">
                <label asp-for="Submission.AssignmentID" class="control-label"></label>
                <input asp-for="Submission.AssignmentID" class="form-control" />
                <span asp-validation-for="Submission.AssignmentID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Submission.StudentID" class="control-label"></label>
                <input asp-for="Submission.StudentID" class="form-control" />
                <span asp-validation-for="Submission.StudentID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Submission.Content" class="control-label"></label>
                <input asp-for="Submission.Content" class="form-control" />
                <span asp-validation-for="Submission.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
